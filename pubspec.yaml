name: gestao_atendimentos
description: Aplicativo de Gestão de Atendimentos com Flutter
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  flutter_bloc: ^8.1.3
  
  sqflite: ^2.3.0
  path: ^1.8.3
  
  image_picker: ^1.0.4
  
  get_it: ^7.6.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true